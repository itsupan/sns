node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# Generated by `wrangler types` during `pnpm install` (see the prepare script)
worker-configuration.d.ts

# OS
.DS_Store
Thumbs.db
Desktop.ini
*~

# Editors (keep the shared VS Code extension recommendations)
.idea
.vscode/*
!.vscode/extensions.json
*.swp
*.swo

# Logs
*.log
logs

# Tool caches
.eslintcache
*.tsbuildinfo

# Local SQLite files (local D1 lives in .wrangler/state)
*.sqlite
*.sqlite3
*.db
*.db-journal

# Private keys and certificates
*.pem
*.key
*.p12
*.crt

# Env
.env
.env.*
!.env.example
!.env.test
.dev.vars
.dev.vars.*
!.dev.vars.example

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright
test-results
playwright-report

# Coverage (Vitest → SonarCloud)
coverage

# SonarCloud scanner cache
.scannerwork

# Claude Code (personal settings; shared project settings stay tracked)
CLAUDE.md
.claude/settings.local.json
