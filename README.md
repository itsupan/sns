# sns

SvelteKit + TypeScript on Cloudflare Workers, with D1 (SQLite), KV and Better Auth.

This is the project skeleton: tooling, bindings and folder structure only — no features yet.
The D1 → Drizzle → `locals.db` chain and the KV binding are covered by the `/api/health`
smoke test. Google sign-in is wired on the server (`/api/auth/*`), with no UI yet.

## Stack

| Concern         | Tool                                                        |
| --------------- | ----------------------------------------------------------- |
| Framework       | SvelteKit 2 (Svelte 5 runes) + Vite 8                       |
| Language        | TypeScript 6 (`svelte-check`)                               |
| Formatting      | Prettier + `prettier-plugin-svelte`                         |
| Linting         | ESLint 10 flat config + `typescript-eslint` + svelte plugin |
| Unit/component  | Vitest 4 (`server` node project + `client` browser project) |
| End-to-end      | Playwright                                                  |
| Database        | Cloudflare D1 (SQLite) via Drizzle ORM                      |
| Key-value store | Cloudflare KV (`platform.env.KV`)                           |
| Auth            | Better Auth + Google OAuth, sessions stored in D1           |
| Hosting         | Cloudflare Workers (`@sveltejs/adapter-cloudflare`)         |
| CI/CD           | GitHub Actions → production                                 |
| Code review     | CodeRabbit (`.coderabbit.yaml`)                             |
| Code quality    | SonarCloud (`sonar-project.properties`) + Vitest coverage   |
| Supply chain    | Aikido Safe Chain (`.aikido`) + `pnpm audit`                |
| Package manager | pnpm                                                        |

## Getting started

**1. Install Safe Chain once per machine** (blocks malicious packages on `pnpm install`),
then restart your terminal:

```sh
curl -fsSL https://github.com/AikidoSec/safe-chain/releases/download/1.5.20/install-safe-chain.sh -o /tmp/install-safe-chain.sh \
  && echo "0ad25efe15d1fa56105157a454d647223e78eb0c53d1f85e3d10afcd722e7bfd  /tmp/install-safe-chain.sh" | sha256sum -c - \
  && sh /tmp/install-safe-chain.sh \
  && rm /tmp/install-safe-chain.sh
pnpm safe-chain-verify
```

**2. Add local secrets.** Copy `.dev.vars.example` to `.dev.vars` and fill it in. For
Google, create an OAuth client (Google Cloud Console → APIs & Services → Credentials →
_Web application_) with these authorized redirect URIs:

- `http://localhost:5173/api/auth/callback/google` (`pnpm dev`)
- `http://localhost:4173/api/auth/callback/google` (`pnpm preview`)

**3. Run it.**

```sh
pnpm install
pnpm db:setup:local   # applies migrations to the local D1 in .wrangler/state
pnpm dev
```

Open <http://localhost:5173>. `pnpm dev` runs your Worker bindings against local D1 and KV,
so `platform.env.DB` and `platform.env.KV` behave the same as they do in production.

## Project layout

```
migrations/            SQL migrations generated by Drizzle, applied by Wrangler
seeds/dev.sql          Idempotent local seed data (empty for now)
src/
  app.d.ts             App.Platform (Cloudflare Env) and App.Locals types
  app.html             HTML shell
  hooks.server.ts      Per-request locals.db / locals.auth / locals.user; serves /api/auth/*
  lib/
    auth-client.ts     Browser Better Auth client (authClient.signIn.social, …)
    assets/            Static assets imported from code (favicon, …)
    components/        Shared Svelte components
    types/             Shared TypeScript types
    utils/             Framework-agnostic helpers
    server/            Server-only code (never shipped to the browser)
      auth.ts          createAuth(env, db) → per-request Better Auth instance
      auth-options.ts  Better Auth options shared with the schema generator
      db/
        schema.ts      Drizzle table definitions — the source of truth
        auth-schema.ts Better Auth tables (generated by `pnpm auth:schema`)
        index.ts       getDb(platform) → typed Drizzle client
      services/        Business logic used by routes
  routes/
    +layout.svelte     Root layout
    +page.svelte       Home page placeholder
    page.svelte.spec.ts  Component test (Vitest, browser project)
    page.svelte.e2e.ts   End-to-end test (Playwright)
    api/health/+server.ts  Smoke endpoint: reports D1 and KV reachability
static/                Files served as-is
wrangler.jsonc         Worker, D1, KV, vars and required secrets for local / production
.dev.vars.example      Template for local secrets (copy to .dev.vars)
drizzle.config.ts      Drizzle Kit — generates SQL only, never talks to D1
better-auth.config.ts  Static auth instance, only for the Better Auth CLI
sonar-project.properties  SonarCloud project settings
.coderabbit.yaml       CodeRabbit review settings
.aikido                Safe Chain settings (minimum package age)
.github/workflows/cicd.yml
```

## Generated files

`worker-configuration.d.ts` (the `Env` type for your bindings) and `.svelte-kit/` are
generated, gitignored, and rebuilt by the `prepare` script on `pnpm install`. `pnpm check`
regenerates the binding types too, so they can never drift from `wrangler.jsonc`. Run
`pnpm gen` yourself after adding a binding if your editor needs it sooner.

## Scripts

| Script                       | Does                                                                  |
| ---------------------------- | --------------------------------------------------------------------- |
| `pnpm dev`                   | Vite dev server with emulated Cloudflare bindings                     |
| `pnpm build`                 | Typecheck bindings and build the Worker into `.svelte-kit/cloudflare` |
| `pnpm preview`               | Serve the built Worker with `wrangler dev` (real workerd)             |
| `pnpm check`                 | `svelte-check` + verify `worker-configuration.d.ts` is current        |
| `pnpm lint` / `pnpm format`  | ESLint / Prettier (`pnpm format:check` for a read-only check)         |
| `pnpm test:unit`             | Vitest (append `--run` for a single pass)                             |
| `pnpm test:coverage`         | Vitest once, writing `coverage/lcov.info` for SonarCloud              |
| `pnpm test:e2e`              | Playwright against a real build served by Wrangler                    |
| `pnpm test`                  | Everything                                                            |
| `pnpm db:generate`           | Generate a migration from `schema.ts` (`--name <label>` optional)     |
| `pnpm db:setup:local`        | Apply migrations to local D1                                          |
| `pnpm db:migrate:production` | Apply migrations to the production D1                                 |
| `pnpm gen`                   | Regenerate `worker-configuration.d.ts` after editing bindings         |
| `pnpm auth:schema`           | Regenerate `auth-schema.ts` after changing `auth-options.ts`          |
| `pnpm audit:deps`            | `pnpm audit`, failing on high or critical advisories                  |

## Changing the database

1. Edit `src/lib/server/db/schema.ts` (or, for auth tables, change `auth-options.ts` and run
   `pnpm auth:schema`).
2. `pnpm db:generate --name <what_changed>` — writes `migrations/NNNN_<name>.sql`.
3. Review the generated SQL, then `pnpm db:setup:local`.
4. Commit the schema **and** the migration. CI applies it to production on deploy.

Drizzle only generates SQL here; Wrangler owns applying it, so migration state is tracked
in D1's `d1_migrations` table rather than by Drizzle.

## One-time setup

Deployment and the external services are not configured until you do these steps.

**1. Create the production D1 database and KV namespace** and paste the returned IDs into
`wrangler.jsonc`, replacing `REPLACE_WITH_PRODUCTION_D1_DATABASE_ID` and
`REPLACE_WITH_PRODUCTION_KV_NAMESPACE_ID`:

```sh
pnpm exec wrangler login
pnpm exec wrangler d1 create sns-prod
pnpm exec wrangler kv namespace create KV --env production
```

The top-level IDs in `wrangler.jsonc` are placeholders on purpose — local D1 and KV live in
`.wrangler/state` and never use them.

**2. Configure auth for production.** Replace `REPLACE_WITH_PRODUCTION_URL` in
`wrangler.jsonc` with the site's URL, add
`<production URL>/api/auth/callback/google` as a redirect URI on the Google OAuth client,
then set the Worker secrets:

```sh
pnpm dlx auth@latest secret | pnpm exec wrangler secret put BETTER_AUTH_SECRET --env production
pnpm exec wrangler secret put GOOGLE_CLIENT_ID --env production
pnpm exec wrangler secret put GOOGLE_CLIENT_SECRET --env production
```

**3. Add GitHub repository secrets** (Settings → Secrets and variables → Actions):

| Secret                  | Where to get it                                                                                                     |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------- |
| `CLOUDFLARE_API_TOKEN`  | Cloudflare dashboard → My Profile → API Tokens → _Edit Cloudflare Workers_ template, with D1 and KV edit permission |
| `CLOUDFLARE_ACCOUNT_ID` | Cloudflare dashboard → Workers & Pages → Account ID                                                                 |
| `SONAR_TOKEN`           | SonarCloud → My Account → Security → Generate token                                                                 |

If you create a GitHub Environment named `production`, the Cloudflare secrets can live there
instead and you can gate deploys behind a required reviewer. `SONAR_TOKEN` must stay a
repository secret, because the `ci` job doesn't run in that environment.

**4. Connect SonarCloud.** Import the repo at <https://sonarcloud.io>, turn off _Automatic
Analysis_ (Administration → Analysis Method), and make sure `sonar.organization` and
`sonar.projectKey` in `sonar-project.properties` match. Without `SONAR_TOKEN` the scan step
is skipped.

**5. Install CodeRabbit.** Add the [CodeRabbit GitHub App](https://github.com/apps/coderabbitai)
to the repo. It reviews every PR using `.coderabbit.yaml`.

**6. Push.** See below.

## CI/CD

`.github/workflows/cicd.yml`:

- **Every push and PR** to `development` or `main` runs: Safe Chain install → dependency
  install → `pnpm audit` → format check → lint → typecheck → unit/component tests with
  coverage → SonarCloud scan → build → end-to-end tests.
- **Push to `main`** → after CI passes, applies migrations to the production D1 and deploys
  the `sns` Worker.

Migrations run _before_ the Worker is deployed, so the new code never meets an old schema.
The audit fails CI on any high or critical advisory, including in dev-only dependencies.

Deploy manually with `pnpm deploy:production` if you need to.
